post_install() {
	echo "bootlin toolchain installed to /opt/xtensa-lx60-uclibc-bleeding-edge"
	echo "The PATH variable will be updated on re-login. To immediately load it:"
	echo ""
	echo "	source /etc/profile.d/xtensa-lx60-uclibc-bleeding-edge-toolchain.sh"
	echo ""
}
