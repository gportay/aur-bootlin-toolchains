post_install() {
	echo "bootlin toolchain installed to /opt/openrisc-uclibc-stable"
	echo "The PATH variable will be updated on re-login. To immediately load it:"
	echo ""
	echo "	source /etc/profile.d/openrisc-uclibc-stable-toolchain.sh"
	echo ""
}
