post_install() {
	echo "bootlin toolchain installed to /opt/m68k-coldfire-uclibc-bleeding-edge"
	echo "The PATH variable will be updated on re-login. To immediately load it:"
	echo ""
	echo "	source /etc/profile.d/m68k-coldfire-uclibc-bleeding-edge-toolchain.sh"
	echo ""
}
