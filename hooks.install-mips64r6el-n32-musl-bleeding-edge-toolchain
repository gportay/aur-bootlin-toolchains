post_install() {
	echo "bootlin toolchain installed to /opt/mips64r6el-n32-musl-bleeding-edge"
	echo "The PATH variable will be updated on re-login. To immediately load it:"
	echo ""
	echo "	source /etc/profile.d/mips64r6el-n32-musl-bleeding-edge-toolchain.sh"
	echo ""
}
