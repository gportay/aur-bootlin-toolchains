post_install() {
	echo "bootlin toolchain installed to /opt/mips64-n32-glibc-stable"
	echo "The PATH variable will be updated on re-login. To immediately load it:"
	echo ""
	echo "	source /etc/profile.d/mips64-n32-glibc-stable.sh"
	echo ""
}
