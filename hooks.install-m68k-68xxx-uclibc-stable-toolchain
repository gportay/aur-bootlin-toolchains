post_install() {
	echo "bootlin toolchain installed to /opt/m68k-68xxx-uclibc-stable"
	echo "The PATH variable will be updated on re-login. To immediately load it:"
	echo ""
	echo "	source /etc/profile.d/m68k-68xxx-uclibc-stable-toolchain.sh"
	echo ""
}
