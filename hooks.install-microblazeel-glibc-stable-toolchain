post_install() {
	echo "bootlin toolchain installed to /opt/microblazeel-glibc-stable"
	echo "The PATH variable will be updated on re-login. To immediately load it:"
	echo ""
	echo "	source /etc/profile.d/microblazeel-glibc-stable.sh"
	echo ""
}
