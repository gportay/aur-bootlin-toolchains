post_install() {
	echo "bootlin toolchain installed to /opt/arcle-hs38-uclibc-stable"
	echo "The PATH variable will be updated on re-login. To immediately load it:"
	echo ""
	echo "	source /etc/profile.d/arcle-hs38-uclibc-stable.sh"
	echo ""
}
