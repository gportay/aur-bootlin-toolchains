post_install() {
	echo "bootlin toolchain installed to /opt/mips64el-n32-musl-stable"
	echo "The PATH variable will be updated on re-login. To immediately load it:"
	echo ""
	echo "	source /etc/profile.d/mips64el-n32-musl-stable-toolchain.sh"
	echo ""
}
