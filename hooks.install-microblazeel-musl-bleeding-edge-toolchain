post_install() {
	echo "bootlin toolchain installed to /opt/microblazeel-musl-bleeding-edge"
	echo "The PATH variable will be updated on re-login. To immediately load it:"
	echo ""
	echo "	source /etc/profile.d/microblazeel-musl-bleeding-edge.sh"
	echo ""
}
