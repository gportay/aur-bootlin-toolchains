post_install() {
	echo "bootlin toolchain installed to /opt/aarch64be-musl-stable"
	echo "The PATH variable will be updated on re-login. To immediately load it:"
	echo ""
	echo "	source /etc/profile.d/aarch64be-musl-stable.sh"
	echo ""
}
