post_install() {
	echo "bootlin toolchain installed to /opt/riscv32-ilp32d-uclibc-stable"
	echo "The PATH variable will be updated on re-login. To immediately load it:"
	echo ""
	echo "	source /etc/profile.d/riscv32-ilp32d-uclibc-stable-toolchain.sh"
	echo ""
}
