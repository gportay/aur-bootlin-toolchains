post_install() {
	echo "bootlin toolchain installed to /opt/armv7m-uclibc-bleeding-edge"
	echo "The PATH variable will be updated on re-login. To immediately load it:"
	echo ""
	echo "	source /etc/profile.d/armv7m-uclibc-bleeding-edge-toolchain.sh"
	echo ""
}
