post_install() {
	echo "bootlin toolchain installed to /opt/powerpc-e500mc-uclibc-bleeding-edge"
	echo "The PATH variable will be updated on re-login. To immediately load it:"
	echo ""
	echo "	source /etc/profile.d/powerpc-e500mc-uclibc-bleeding-edge-toolchain.sh"
	echo ""
}
