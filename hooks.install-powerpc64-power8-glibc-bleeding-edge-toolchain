post_install() {
	echo "bootlin toolchain installed to /opt/powerpc64-power8-glibc-bleeding-edge"
	echo "The PATH variable will be updated on re-login. To immediately load it:"
	echo ""
	echo "	source /etc/profile.d/powerpc64-power8-glibc-bleeding-edge-toolchain.sh"
	echo ""
}
